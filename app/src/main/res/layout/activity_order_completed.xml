<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/button_background"
    tools:context=".ui.OrderCompletedActivity">

    <ImageView
        android:id="@+id/orderCompletedImageView"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:elevation="25dp"
        android:src="@drawable/ic_baseline_done_24"
        app:layout_constraintBottom_toTopOf="@+id/orderCompletedTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:tint="@color/black" />

    <TextView
        android:id="@+id/orderCompletedTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:elevation="48dp"
        android:fontFamily="@font/montserrat"
        android:text="@string/order_completed_success"
        android:textColor="@color/black"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/orderCompletedImageView" />
</androidx.constraintlayout.widget.ConstraintLayout>